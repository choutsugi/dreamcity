version: '3'
services:
  redis:
    image: redis:latest
    restart: "always"
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - ./conf/redis.conf:/etc/redis/redis.conf
      - ./data/redis:/data
    command: redis-server /etc/redis/redis.conf
    privileged: true


